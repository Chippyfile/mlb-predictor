// src/sports/nfl/nflSync.js
// Stub — NFL auto-sync placeholder. Real sync runs via cron during season.
export async function nflAutoSync(onMsg) {
  onMsg?.("🏈 NFL sync: predictions loaded from Supabase");
}
